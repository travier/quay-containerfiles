FROM registry.fedoraproject.org/fedora:36 as unzip

# From https://www.consulproject.io/downloads
ENV NOMAD_VERSION="1.13.2"
ENV NOMAD_SHA256="a72e88cbfec6c0fb3620cd58314ff0b42fc9d605a5192d6a568a417180f0b35f  consul_1.13.2_linux_amd64.zip"

RUN dnf -y update && \
    dnf -y install unzip && \
    dnf clean all

RUN curl -O "https://releases.hashicorp.com/consul/${CONSUL_VERSION}/consul_${CONSUL_VERSION}_linux_amd64.zip"
RUN echo "${CONSUL_SHA256}" | sha256sum --check
RUN unzip consul_${CONSUL_VERSION}_linux_amd64.zip

FROM scratch

# Keep container image for ~2 months
LABEL quay.expires-after=8w

COPY --from=unzip /consul .
CMD ["/consul"]
